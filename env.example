# All API keys are entered via the Gradio UI at runtime.
# No environment variables are required to start the app.
#
# However, you can optionally set defaults here:
#
# OPENAI_API_KEY=sk-...
# MEM0_API_KEY=m0-...
